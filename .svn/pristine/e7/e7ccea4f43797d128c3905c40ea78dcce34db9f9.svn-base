import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { EnTeteLigneComponent } from './en-tete-ligne.component';

describe('EnTeteLigneComponent', () => {
  let component: EnTeteLigneComponent;
  let fixture: ComponentFixture<EnTeteLigneComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ EnTeteLigneComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(EnTeteLigneComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
