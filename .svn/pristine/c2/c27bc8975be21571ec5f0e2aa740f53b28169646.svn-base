import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AddUpdateConventionComponent } from './add-update-convention.component';

describe('AddUpdateConventionComponent', () => {
  let component: AddUpdateConventionComponent;
  let fixture: ComponentFixture<AddUpdateConventionComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AddUpdateConventionComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AddUpdateConventionComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
