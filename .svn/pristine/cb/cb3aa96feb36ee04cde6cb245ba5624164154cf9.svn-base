import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PrimConventionComponent } from './prim-convention.component';

describe('PrimConventionComponent', () => {
  let component: PrimConventionComponent;
  let fixture: ComponentFixture<PrimConventionComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PrimConventionComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PrimConventionComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
